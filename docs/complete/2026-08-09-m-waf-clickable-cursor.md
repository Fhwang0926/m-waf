# 클릭 가능 요소 포인터 표시

## 완료 내용

- 링크, 활성 버튼, 접기·펼치기 영역, 선택 상자, 체크·라디오 입력과 클릭 가능한 이벤트 행에 포인터 커서를 공통 적용했다.
- 비활성 버튼과 입력은 기존 `not-allowed` 표시를 유지한다.

## 확인 범위

- `git diff --check`로 CSS 형식을 확인했다.
- 저장소 지침에 따라 프론트엔드 build/test는 실행하지 않았다.
